const Login = () => {
  return (
    <div>
      <input type="text" aria-label="email" />
      <input type="text" aria-label="user-password" />
      <button aria-label="login">login</button>
    </div>
  );
};

export default Login;
